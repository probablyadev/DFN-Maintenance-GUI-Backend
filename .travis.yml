sudo: true

language: python

python:
  - 3.6

os:
  - linux

cache: pip

notifications:
  email: false

stages:
  - test
  - name: deploy
    if: branch = master AND type = push AND fork = false

install: pip install nose python-semantic-release


jobs:
  include:
    - stage: test
      script: nosetests

    - stage: deploy
      deploy:
        provider: script
        skip_cleanup: true
        script: bash scripts/deploy.sh
