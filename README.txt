--Fireball Camera Network Maintenance GUI using web.py and HTML/CSS--

This repository will hold the code for the GUI which will be used to maintain the camera network for the Fireballs in the Sky project.
Repo is public for now, but will be privatised if requested.